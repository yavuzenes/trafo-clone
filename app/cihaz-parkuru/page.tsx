import type{Metadata}from"next";import Image from"next/image";import{Header,Footer,Breadcrumb}from"../components";import{equipment}from"../seo-data";
export const metadata:Metadata={title:"Cihaz Parkurumuz | Trafo Test ve Ölçüm Cihazları",description:"BES Enerji'nin trafo, kesici, izolasyon, yağ ve topraklama testlerinde kullandığı ölçüm cihazlarını inceleyin.",alternates:{canonical:"/cihaz-parkuru"}};
export default function Page(){return <><Header/><main><section className="page-hero light-hero"><div className="shell"><Breadcrumb items={[{label:"Cihaz Parkurumuz"}]}/><span className="kicker">Ölçülebilir teknik sonuçlar</span><h1>Saha testleri için profesyonel cihaz parkuru.</h1><p>Word çalışmasında belirtilen Multitech cihazları, doğru model görselleri ve temel kullanım kapsamlarıyla listelenmiştir.</p></div></section><section className="section"><div className="shell equipment-grid">{equipment.map(item=><article key={item.name}><div className="equipment-image"><Image src={item.image} alt={`${item.code} ${item.name}`} fill sizes="(max-width:700px) 100vw, 33vw"/></div><div className="equipment-summary"><span>{item.code}</span><h2>{item.name}</h2></div><div className="equipment-overlay"><h3>{item.name}</h3><ul>{item.specs.map(spec=><li key={spec}>{spec}</li>)}</ul><a href={item.source} target="_blank" rel="noreferrer">Ürün kaynağını inceleyin →</a></div></article>)}</div><p className="equipment-note">Görseller, üreticinin Türkiye ürün sayfalarından alınmıştır. Sahada kullanılacak cihaz ve test kapsamı ekipman tipine göre netleştirilir.</p></section></main><Footer/></>}
