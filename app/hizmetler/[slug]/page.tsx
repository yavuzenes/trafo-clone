import type {Metadata} from "next"; import Image from "next/image"; import {notFound} from "next/navigation"; import {Header,Footer,Breadcrumb} from "../../components"; import {services,company} from "../../data";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)return {};return {title:`${s.title} | Ankara Trafo Servisi`,description:s.summary,alternates:{canonical:`/hizmetler/${s.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const schema={"@context":"https://schema.org","@type":"Service","name":s.title,"description":s.summary,"provider":{"@type":"Organization","name":"BES Enerji","telephone":company.phone},"areaServed":"Türkiye"};return <><Header/><main><section className="service-hero"><div className="shell"><Breadcrumb items={[{label:"Hizmetler",href:"/hizmetler"},{label:s.title}]}/><div className="service-hero-grid"><div><span className="kicker light">{s.eyebrow}</span><h1>{s.title}</h1><p>{s.intro}</p><div className="actions"><a className="btn primary" href={`tel:${company.phone}`}>Teknik ekibi arayın</a><a className="btn ghost" href="https://wa.me/905532124761" target="_blank" rel="noreferrer">WhatsApp</a></div></div><div className="service-hero-image"><Image src={s.image} alt={s.title} fill priority sizes="(max-width:760px) 100vw, 45vw"/></div></div></div></section><section className="benefits"><div className="shell">{s.benefits.map((b,i)=><div key={b}><strong>0{i+1}</strong><span>{b}</span></div>)}</div></section><section className="section"><div className="shell content-grid"><div><span className="kicker">Hizmet kapsamı</span><h2>Sahada uyguladığımız kontroller</h2><p className="lead">Kapsam, keşif sonrasında ekipman ve tesis koşullarına göre kesinleştirilir.</p></div><ul className="check-list">{s.scope.map(x=><li key={x}>{x}</li>)}</ul></div></section><section className="soft-section"><div className="shell"><span className="kicker">Nasıl çalışıyoruz?</span><h2>Planlı, güvenli ve izlenebilir süreç.</h2><div className="process-grid">{s.process.map((p,i)=><article key={p.title}><span>0{i+1}</span><h3>{p.title}</h3><p>{p.text}</p></article>)}</div></div></section><section className="section"><div className="shell faq-grid"><div><span className="kicker">Sık sorulan sorular</span><h2>{s.shortTitle} hakkında merak edilenler.</h2></div><div>{s.faq.map(x=><details key={x.q}><summary>{x.q}</summary><p>{x.a}</p></details>)}</div></div></section></main><Footer/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></>}
