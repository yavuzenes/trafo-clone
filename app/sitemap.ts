import type {MetadataRoute} from "next";import {services} from "./data";import {locales,translatedServices} from "./localized";
export default function sitemap():MetadataRoute.Sitemap{const base="https://besenerji.net",tr=["","/hizmetler","/kurumsal","/projeler","/iletisim",...services.map(s=>`/hizmetler/${s.slug}`)],localized=locales.flatMap(l=>[`/${l}`,`/${l}/${l==="en"?"services":"الخدمات"}`,`/${l}/${l==="en"?"about":"عن-الشركة"}`,`/${l}/${l==="en"?"projects":"المشاريع"}`,`/${l}/${l==="en"?"contact":"اتصل-بنا"}`,...translatedServices[l].map(s=>`/${l}/${l==="en"?"services":"الخدمات"}/${s.slug}`)]);return [...tr,...localized].map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:url===""?"weekly":"monthly",priority:url===""?1:.8}))}
